# Example: ordered first-time setup with .scaffl/install/ scripts.
#
# Run `scaffl install` from this directory to walk through the
# numbered steps under .scaffl/install/. Each step writes a marker
# file under `state/` so you can see exactly which steps ran.
#
# Try:
#   scaffl install                       # full plan
#   scaffl install --list                # plan + last status per step
#   scaffl install --dry-run             # preview without executing
#   scaffl install 03-seed-db            # single-step mode
#   scaffl install --restart             # wipe state, run from step 1

[project]
name        = "install-flow-demo"
description = "Walk-through of scaffl install"

[containers]
backend = "none"             # host-only; no compose preflight

# `install_with_setup = false` keeps things demo-focused; in real
# projects you usually want the default (true).
[agents]
install_with_setup = false

[install]
install_git_hooks = false    # the hooks example covers that surface
