//! Control-center view: the home dashboard listing services,
//! watchers, recipes, scripts, and the synthetic runtime row.
//!
//! Phase 1 has only the leaf state types; later phases will move
//! the render and input dispatch into sibling files.

pub mod state;
